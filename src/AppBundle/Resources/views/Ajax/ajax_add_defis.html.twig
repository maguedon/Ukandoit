
{% block javascripts %}
<script type="text/javascript">

	{% if data.error is defined %}
		toastr.error("Vous n'avez aucune performance liée à cette date !");
	{% else %}
	if("{{ data.pas_or_km }}" == "pas"){
		$("#formOne_nbSteps").val({{ data.nbPas }});
		$("#formOne_nbSteps").attr("placeholder", {{ data.nbPas }});

		$("#formOne_kilometres").val(null);
		$("#formOne_kilometres").attr("placeholder", "Pas concerné");
	}
	else if("{{ data.pas_or_km }}" == "km"){
		$("#formOne_kilometres").val({{ data.nbKm }});
		$("#formOne_kilometres").attr("placeholder", {{ data.nbKm }});

		$("#formOne_nbSteps").val(null);
		$("#formOne_nbSteps").attr("placeholder", "Pas concerné");
	}

	$("#formOne_time").val({{ data.time }});
	$("#formOne_time").attr("placeholder", {{ data.time }});

	Materialize.updateTextFields();

	$('#hidden_content').removeClass("hide");
	$('#ToDisabledAddDefis').removeAttr('disabled');
	{% endif %}

</script>
{% endblock %}
